# 本次 GitHub 上传范围

归档日期：2026-09-14。此仓库以已封版 1.5.0 为初始快照，新仓库不继承其他工作台或产品仓库历史。

## 文件来源

- 根目录的 Skill、bridge、示例与包文档：按原包 `SHA256SUMS.txt` 的 87 项白名单复制，上传前核对哈希。
- `dist/prototype-requirement-workflow-1.5.0.zip`：原始未运行的完整分发包，493355 字节。
- 根目录 README 和 docs：本次整理的使用入口、脱敏对话修改记录和待实现方案。
- `.gitignore`：阻止常见私有运行数据和凭证进入后续提交；不能代替每次人工审查。
- `.gitattributes`：禁止换行自动转换，以保留原包清单的逐字节完整性。

ZIP SHA-256：`D48AA9A8A704D240298D204AF1B63F26502D78D29A64D235F4D3525C3ACC1803`。

`SHA256SUMS.txt` 只描述原包，不描述额外仓库文件。后续代码变更应发布新版本及新清单，不偷偷更改已封版 ZIP 或宣称旧清单仍覆盖新内容。开发前可重新安装锁定的 npm 依赖；node_modules 不提交。

## 不上传

真实 RP/rplib、原稿/扫描文本/私人截图、完整聊天及工具日志、session/token、Axure 映射、项目配置、本机 local-runtime、运行 history/output、账号凭证、公司数据、Axure 安装程序、无关工作台代码。合成示例及已列入原包白名单的示例预览图保留。

桥接 DLL 为工作流适配组件，源码随包；不代表 Axure 官方授权或产品背书。不附第三方软件许可证或模型账号。此次创建私有仓库，不自动添加开源许可证或公开分发权限。

## 验证口径

上传前在此新目录安装锁定依赖，运行 52 项自动测试；核对原包内容清单与 ZIP 哈希。规划/编译测试不等于视觉或产品验收。

本次上传不启动 Axure、不改真实 RP、不重跑三版原生验证。1.5.0 新说明/表格的原生显示和增量修改仍待测试。详情见根目录验证记录。

对话记录按阶段整理，只保留与实现/决策相关的脱敏信息。最末讨论的“模糊输入引导”仅在待实现文档中保留，没有写入已发布 Skill。
