# 脱敏合成示例，不是实际用户业务或历史原文

任务列表 Demo：绘制 Home 和 History 两个功能页，采用安卓评审样式。失败任务在两页同步显示 Failed；点击 History 的 Retry 重试当前任务，成功后两页显示 Done，不新增记录。本次不绘制 Detail，但在说明中登记其同步影响。

模拟确认：新版 v1.0.6 的任务身份规则回退到 v1.0.1；不沿用 v1.0.2 的“每次重试新增记录”。History 按钮从 Try Again 改为 Retry，其余明确保留。美术仅需区分失败与成功状态，无需新增插画。

这些文字是公开虚构测试输入，sources 中 user/baseline 为模拟标签，不冒充真实确认。用于验证模型与输出，不用于宣称产品效果。
