# Test Fixtures

本目录只供自动测试和兼容性回归。禁止将这里的产品名称、页面结构、按钮、状态和需求说明用作真实原型的参考来源。

- `clarification-home/` 验证必要问题、来源、规划门禁和原文一致性。
- `legacy-printer/` 保留旧 Printer scene，用于确认历史数据和桥接仍可运行。

真实需求应读取 `../README.md`、`../reference/` 和 skill 的 `references/rp-pattern-library.md`。除非任务是运行测试或排查兼容问题，Agent 不读取本目录。
