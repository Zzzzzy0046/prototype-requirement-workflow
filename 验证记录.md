# 完整工作流包验证记录

# 1.9.2 独立控件与发布包复验

日期：2026-09-15。

- 仓库运行 `node --test bridge/scene.test.mjs bridge/planning.test.mjs bridge/evolution.test.mjs`，64/64 项通过。
- 仓库 `prototype-requirement-writer` 运行 `quick_validate.py`，结构校验通过。
- `prototype-requirement-workflow-1.9.2.zip` 的 SHA-256 与 sidecar 一致；ZIP 内 68/68 个 manifest 文件哈希匹配，禁带文件 0。
- 改动文件名和文本扫描未发现 `.env`、凭证、token、本机会话、`axure-map.json`、用户 RP/RPLIB、临时截图路径或本机绝对路径。
- 本轮发布复验未重新启动 Axure，不能据此宣称重新完成 4134、4137、4149 三版本原生验收；真实 Tag Finder 项目的原生写入属于项目验证，不进入公共分发包。

# 1.9.0 功能框架与竞品截图双输入门禁

日期：2026-09-15

- 新增 `framework-screenshot` 工作流 profile：产品功能框架决定页面范围与页面分布，用户手动上传的竞品截图必须以 `competitor-abstraction` 记录页面类型、主任务、布局、模块、控件和状态。
- `page-plan.json` 增加 `stage`、`gates` 和页面级 `frameworkSourceIds`；来源增加 inputRole，区分产品功能框架、竞品截图、团队 RP 模板、页面清单确认和页面画面确认。
- `page-design` 允许 scene.notes 为空，只生成页面；`deferred-note` 仅用于标记页面确认后再写的规则。`final` 要求两道真实用户确认，恢复 notes、coverage 和行为说明校验。
- 缺少框架、竞品截图、低保真抽象、页面清单确认或页面画面确认时，规划门禁拒绝通过；竞品截图仍是 reference，不能成为 confirmed 业务规则来源。
- Bridge 自动测试 64 项全部通过，其中新增 4 项覆盖双输入、两道确认门、阶段说明隔离和 final 说明恢复。Skill 结构校验通过后再封装 1.9.0。
- 本轮没有启动 Axure、没有修改真实 RP、没有重新执行 4134/4137/4149 原生写入；代码测试不能代替后续真实页面视觉验收。

# 1.8.1 实际 RP 页面直接复刻门禁

日期：2026-09-15。

- Skill 新增 `direct-rp-replica` 路由：真实 RP 页面优先，功能框架、流程图、竞品截图和版本信息不能代替 UI 主参考；用户未要求时不生成 Product Overview。
- page plan 可记录源页面、控件读取、原生渲染和五个复刻维度；直接复刻至少需要四项维度，缺少读取或渲染证据会被规划校验阻止。
- Smart Printer 试跑已从 16 页调整为 15 个业务规划页，并用 Printer RP 的 9 个需求详情页生成原生参考预览；功能框架与启动流程仅保留为业务证据。
- 仓库 60/60 项 Node 自动测试通过；新增直接复刻证据正反例。仓库 Skill 和本机安装 Skill 均通过 quick_validate。
- 本轮没有写入或修改用户 RP。预览中的 RP 原生渲染仅用于选型与视觉对照；最终 Axure 交付必须使用独立可编辑控件，不能把截图写进 RP。

# 1.8.0 参考驱动需求与控件质量门禁

- 新任务使用 requirements-model/page-plan/scene version 2；旧 version 1 继续可读并明确提示未经过新质量门禁。
- version 2 已验证逐页 referenceSelections、high-consequence assumption 拦截、完整产品 assumption 过载、note coverage、componentRole、screen 语义控件密度和 visualChange/static-board 规则。
- 新增脱敏正例 `examples/reference/requirement-depth-standard/`，model/plan/scene 联合校验无错误和警告；HTML preview 已生成并人工核对页面说明、页面进入、交互说明、边界&异常、美术交付、插画、遮罩和系统弹窗层级。
- 59 项 Node 自动测试全部通过；仓库 Skill 通过 quick_validate（使用 UTF-8 模式）。
- 本轮未启动 Axure、未修改 RP、未重测三个 Axure 构建；1.8.0 沿用既有 4134/4137/4149 适配器与历史边界。

# 1.7.6 中性用户入口

- 包根目录 `给Agent的开始提示词.md` 和完整说明的日常示例由 Printer 改为中性的文件扫描工具 Home，避免 test fixture 继续成为默认产品锚点。
- reference 模板请求不再点名单一示例产品；Printer 只保留在 `examples/test-fixtures/legacy-printer/`、兼容代码和历史验证记录中。
- 1.7.5 保持封存；本轮未修改 Axure 写入协议、适配器和 RP，也未启动 Axure。
- 仓库与本机安装 Skill 均通过结构校验；54 项 Node 自动测试和 reference 六页 HTML preview 通过。

# 1.7.5 Examples 分级

- 示例目录分为 `examples/reference/` 与 `examples/test-fixtures/`；根目录 README 明确使用边界和优先级。
- 通用模板、双状态布局和共享规则进入 reference；规划门禁示例和旧 Printer 进入 test-fixtures。旧 Printer 仍可做兼容测试，但不能作为真实业务参考。
- `review-template-library` 新增 `reference-selection.md`，说明每个演示页的主模式、借用范围和禁止继承内容。
- 测试代码、Skill reference、README 和构建脚本已切换到新路径；构建脚本会把旧清单路径迁移到新目录，不递归扫描工作区。
- 本轮未修改 Axure 写入协议、适配器和 RP，也未启动 Axure。
- 仓库与本机安装 Skill 均通过 `quick_validate.py`；54 项 Node 自动测试通过。新路径下 `review-template-library` 成功生成 6 页 HTML preview，`axureWritten=false`。

# 1.7.4 可分发 RP 模式库

- 将此前走查的 32 份多产品 RP、267 个页面记录（201 页结构化解析）脱敏归纳成包内 `references/rp-pattern-library.md`；原始 RP、原文、截图、路径和业务值不进入分发包。
- 模式库提供 12 类页面结构、6 类说明块、内容密度、必要提问及禁止迁移规则；所有使用者无需本机私有索引即可使用。
- 参考路由默认优先当前有效基线，其次使用包内模式；一次只选一个主模式、最多两个辅模式。完整基线负责整体信息架构，局部迭代/Bugfix 只作用于声明范围。
- Mobbin 仍是可选外部行业参考，不能覆盖用户的 `house-review` 生成规范；用户本轮提供的 RP 仍需独立做来源和版本甄别。
- 本轮只修改 skill、reference 与分发元数据，不修改 Axure 写入协议、三个精确版本适配器或现有 RP；未启动 Axure。
- 仓库 Skill 与本机安装 Skill 均通过 `quick_validate.py`；54 项 Node 自动测试通过。
- 1.7.4 分发包应包含模式库及全部通用运行文件，不包含 RP、RPLIB、私有索引、local-runtime、源路径、原文、截图、session、映射或依赖缓存；实际 ZIP 哈希与干净解压复验记录在 `dist/RELEASES.md`。

## 1.7.3 同类参考库与单页密度

日期：2026-09-15。

- 新增同类参考库路由：有效基线、本地同类 RP、跨平台同类、同页面类型、Mobbin、通用 fallback 分层选择；一个主参考，最多两个辅参考。
- Mobbin 仅作为可选外部结构参考，最终输出继续使用用户模板或 `house-review`；包内不包含 Mobbin 连接、账号、截图或私有素材。
- 增加单页密度门槛：`只做 Home` 不等于标题加单 CTA；对当前状态/内容、首要任务、直接相关辅助入口、任务完成后的承接信息、必要导航和结构性状态逐项判断。
- 参考启发的低风险业务模块按 assumption 披露；价格、权益、权限后果、广告、成功判定、删除和数据归属等关键规则仍需有效来源或必要提问。
- 本机安装版登记私有参考索引，覆盖此前授权走查的产品族；私有路径、RP、提取目录和内容均不进入分发包。
- 本轮仅修改需求理解、参考选择和写作规则；未修改 Axure 适配器、MCP 写入协议或现有 RP。
- 仓库与本机安装 Skill 均通过 `quick_validate.py`；54 项自动测试通过。1.7.3 干净解压后 97/97 个清单文件哈希匹配，从解压目录安装依赖并再次通过 54 项测试、Skill 校验和六页模板 HTML preview。
- 分发 ZIP 共 98 个条目（97 个清单文件加 `SHA256SUMS.txt`），未包含 RP、RPLIB、私有参考索引、local-runtime、Mobbin 账号/截图、session、映射或依赖缓存。本轮未启动 Axure，不声称重新验证三版本原生写入。

## 1.7.1 业务说明目录与单一产品去偏

日期：2026-09-15。

- 复核 32 个用户 RP 的既有只读盘点结论，将规则拆成“跨产品硬骨架、按需说明维度、当前产品专属内容”；单一产品只可作为案例，不能独立决定业务章节、状态机或控件样式。
- 内部输入示例不再默认从 Printer 开始。每个功能页先从当前页面的用户任务、业务对象、关键动作、状态差异、数据影响和边界生成专属说明目录；页面说明、页面进入、交互说明、异常处理仅按需出现。
- 当前 10 页模板把泛化标题替换为任务卡、记录筛选、字段编辑、设置项及系统权限返回等对象化标题；页面、控件和说明 key 保持不变。
- requirements-model/page-plan/scene 联合校验通过，无错误或警告；54 项自动测试全部通过；仓库和本机安装 Skill 均通过 `quick_validate.py`。
- Axure 11.0.0.4137 在同一个 RP 中完成 8 个页面批次的原生增量更新、保存、文字与几何回读及整页原生渲染。共 10 页、281 个独立叶子控件，映射中无 Group；目视检查修改页未见说明重叠或截断。重复 apply 为 0 个写入批次，稳定复用现有页面和控件 ID。
- 未重新执行 4134/4149 原生测试；底层版本范围仍为 4134、4137、4149，不据此扩展到所有 Axure 11。1.7.0 ZIP 保持封存。

## 1.7.0 用户 RP 派生的通用模板

日期：2026-09-15。

- 只读遍历用户提供的五组目录，共 32 个 RP、267 个页面记录；201 页完成结构化解析，累计约 3.7 万控件。全部源文件解析前后 SHA-256 一致。
- 对 8 个代表 RP 进一步读取字体、样式、几何、Layer 和 Table。稳定结论写入 `references/house-review-profile.md`；原文、扫描报告和缩略图不进入分发包。
- 新增 `house-review`；`android-review` 作为旧项目兼容别名。新 profile 允许 360px 长页，继续校验白底、#797979 直角框、Arial 13.5pt Bold 评审标题和 22/26px 编号。
- 新增需求概览、单页面、同页多状态、流程、局部迭代/Bugfix、共享规则表格六页模板。requirements-model、page-plan 和 scene 联合校验通过，无错误或警告；HTML 预览生成 6 页。
- 自动测试由 52 项增至 53 项，全部通过。新增测试覆盖 house-review 与旧 android-review 的兼容及 360px 长页。
- 本轮三次原生启动均在已有 `Printer-Skill-Demo` 运行时返回 STARTUP_NOT_READY；新建测试进程均由 Agent 按 PID 核对后关闭，三个目标 RP 哈希保持不变。未执行 apply，不能宣称六页已写入 Axure。需要保存并关闭现有 Axure 主实例后继续。

## 1.6.0 模糊输入主动收敛

2026-09-15：

- 新增 guided-intake.md 并接入 Skill/input-contract/clarification。区分方案探索、按稿执行、局部修改、虚构示例；缩短开始提示词。目标 RP 未提供不阻塞范围内的需求理解和本地预览。
- 不新增 schema、问卷引擎或 Axure 能力；bridge 除 history.mjs 中包版本标识外未修改。原有 52 项自动测试全部通过，Skill 结构校验通过。
- 三个独立 Agent 未继承主对话历史，使用合成输入共六轮试跑；未给预期答案。仅在隔离输出目录生成，不调用真实用户问题工具、Axure、浏览器或外部服务。
- 一句话 Printer 需求：提出简短推荐，只问两种打印入口是否都包含；未索要完整 PRD 或 RP 路径。“继续”未冒充确认；取得明确答复后生成单 Home、指定文案和中文说明，未扩展下游。
- 完整 Paper Shelf 需求：不额外提问，直接出 Home；Reader 内部规则明确排除，不成为阻塞项。
- 局部修改：Report.pdf → Annual Report.pdf；主 Agent 对比前后 scene，确认恰好只有该字符串替换，page/widget/note key、布局及其余规则不变，说明同步。
- 虚构任务列表：直接生成单页，Agent 自主定义的任务内容、顺序、入口细节均标 assumption；与 scene.assumptions 一致，未伪造用户确认。
- 三组生成结果的模型/计划/scene 校验均通过；主 Agent 阅读源数据和说明核对范围、文案和来源状态。该结果不是同事设备、多模型、大样本或统计对照测试，不计算提效比例。
- 本轮未做 HTML/Axure 目视验收，未启动或修改真实 RP，未重跑三版本原生测试。新说明/表格的原生显示与编辑验证仍待完成。

重跑程序：`node --test bridge/scene.test.mjs bridge/planning.test.mjs bridge/evolution.test.mjs`。行为试跑不是单元测试，测试请求与方法见仓库 docs/INTAKE-EVALUATION.md；原始运行数据保持私有，不进入分发包。

## 1.5.0 来源、共享规则、说明表达与修订证据

2026-09-14：

- 在原 32 项基础上增加 20 项回归，52 项全部通过。覆盖一条需求多功能页、遗漏影响页、明确范围外理由、同产品回退、异产品继承拒绝、关系环、待讨论/占位/旧条目、字段来源、停用规则、行为必须写说明、表格精确保留及结构错误、Markdown/HTML 转义、历史快照和实际输出状态。
- 新增虚构 Task Board 两页示例，模型/计划/scene 校验与离线预览成功；Home、History 共享一个规则 id，Detail 仅登记影响，未自动扩大绘制范围。共享/改动/美术说明及表格编译为基础控件；Markdown 保留表格行列。
- 本地 HTML 浏览器目视检查被 URL 安全策略阻止，未绕过，视觉验收未完成；不把几何/编译测试冒充看图结果。
- 每轮执行新增项目私有输入快照、字段差异、结果状态和执行段计时。未知/失败/预览和原生写入分开；不记录 session、map 或命令参数，不自动推断人工提效。
- 本轮没有启动 Axure、没有写入真实 RP，没有重新验证三版本原生操作。底层适配器沿用历史验证；新说明/表格在 Axure 的实际字体、层级和修改体验仍需原生验收。
- 本版只提炼通用结构和合成案例，不包含真实 RP、素材扫描、第三方任务原文或公司指标。旧 1.4.0 ZIP 保留。

重跑：`node --test bridge/scene.test.mjs bridge/planning.test.mjs bridge/evolution.test.mjs`。

## 1.4.0 需求理解、必要提问与表达规划

2026-09-14：

- 增加 requirements-model / page-plan 结构化模型，以及有来源的提问、答复、延期与部分交付规则。主入口路由到新参考文件。
- 新增可选 android-review 说明样式；字体/几何依据已读取的安卓库提取记录，只有抽象参数进入通用包。没有导入私人库、原稿或原生实例。
- Node 自动测试 32 项通过：含旧布局回归、来源引用、问答门禁、延期/假答复拒绝、需求覆盖、部分范围、原文逐字核对、越界新增、目标映射和模板样式检查。
- 集成反例在不存在的 session/RP 路径上运行 apply；未回答的关键问题在连接前被拒绝，没有创建 HTML 或 axure-map。缺一规划文件与显式指定不存在规划文件均被拒绝。
- 虚构 Home 案例通过模型/计划/scene 校验及离线 preview。示例问答明确标注模拟，不冒充实际用户答复。
- 已查看新示例的 HTML 截图：360×640 画框、框外标题、四组编号说明完整可见，无明显截断重叠。没有用 HTML 检查冒充 Axure 原生渲染验证。
- Skill 使用 Python UTF-8 模式运行 quick_validate.py 通过。结构校验不证明自然语言理解或来源真实性。
- 本版不修改 MCP、启动 helper、C# 或三版本适配器。三版原生操作验证沿用 1.3.0 历史记录，本轮没有启动 Axure 或修改真实 RP。

重跑：`node --test bridge/scene.test.mjs bridge/planning.test.mjs`；当前测试夹具见 `examples/test-fixtures/clarification-home/`。

## 1.2.0 规则深化与说明布局验证

2026-09-14：

- 基于经授权的四组真实产品原型提炼规则；分发包不包含这些原稿、导出文本、图片、私有路径或分析报告。正式需求、历史迭代、竞品/预研和空模板分别处理。
- 写作规则改为按交付形态与模块组织，增加来源/版本甄别和按需专项参考；保留唯一入口名、静态画板、原文保留及同一 RP 更新约定。
- 5 组 Node 自动测试通过：旧布局/原文、同功能页双状态、窄列长标题/正文、说明重叠拒绝、越界/重复 key/过长正文拒绝。校验器不能保证内容决策正确。
- 原有 Printer 示例的编译结果与 1.1.0 逐项一致，未改变已有控件 key 或布局。
- 新增虚构文件库示例：一个 Home 功能页、两个静态状态、各自的说明组，共 23 个原生控件；HTML 全页截图与 Axure 原生整页渲染均已目视检查。
- 在独立测试副本中通过 MCP 写入并回读全文及几何信息。移动说明组并修改正文后仍使用同一页/控件 ID，1 个更新批次；重复运行 0 批次；恢复原样例后保存关闭测试副本。
- Skill frontmatter 校验通过。新样例是人工整理的规则示例，不是独立模型或同事环境的自然语言生成评测。

底层桥接未修改，本次没有重新验证其他 Axure build、也没有声称 RP9 可写。本次原型阅读采用副本，不代表跨版本文件写入兼容。自动启动的完整生命周期验证见下方 1.1.0 记录。

复测排版：安装依赖后运行 `node --test bridge/scene.test.mjs`；虚构示例现位于 `examples/reference/annotation-layout/`，其中 `preview/axure-home.png` 为实测原生截图。

## 1.1.0 自动启动验证

2026-09-14，独立临时目录、已保存空白测试 RP、Axure 11.0.0.4149：

- 无依赖首次运行：自动安装依赖，自动选择空闲端口并启动，返回 ready=true / started。
- 再次运行：返回 reused，PID 不变，不重装依赖、不重复启动。
- 自动启动后写入 Home / Connect Printer，34 个控件和整页渲染通过。
- 正常保存关闭后：自动恢复原 RP，返回 resumed；再次执行页面内容更新为 0 批，未重复创建。
- 缺少目标 RP、目标 RP 被文件锁占用：分别返回 PROJECT_REQUIRED / PROJECT_BUSY，未启动新 Axure。
- 新会话保存在当前项目 .axure-session，保留已有项目的 sessionFile。Agent 调用 helper，用户日常无需执行启动命令。

普通 Axure 窗口占用检测结合进程路径、窗口标题和文件锁，为保守检查；窗口无法识别的特殊环境仍可能需要人工核实。没有强制关闭用户窗口或绕过版本限制。

验证日期：2026-09-14。环境：Windows、Axure RP 11.0.0.4149、.NET 10.0.10、Node.js 24.13.0、PowerShell 7。

## 本轮已验证

1、从独立临时目录安装 npm 依赖、安装项目内 skill、启动 Axure 并通过只读连接自检。入口名称为 prototype-requirement-writer。
2、根据虚构 Printer 需求整理两页 scene，生成离线 HTML、中文说明和任务简报；用无界面 Chrome 渲染查看，左右布局和中文显示正常。
3、由 MCP 新建 Home、Connect Printer 两个页面，写入 34 个原生可编辑控件。保留空白 RP 原有 Page 1，共三个页面树条目。
4、将 Documents 改为 Print Documents，同时更新右侧交互说明和验收文案，原位批量更新成功，所有页面 ID 和控件 ID 不变。
5、同一 scene 重复执行，新增/修改批次数为 0，没有重复创建页面或控件。
6、逐项回读所有控件的文案及位置尺寸，与 scene 对照通过；完整段落和换行保留。
7、模拟 Axure 内已有修改，runner 拒绝覆盖；撤销测试修改后恢复运行。尝试删除已映射控件时，在写入前拒绝。
8、正常保存、关闭测试文档、重新加载后，全部映射指纹一致，34 个控件可继续读取和修改。重开后同一 scene 执行仍为 0 次新增/修改。
9、修复跨页渲染对临时 group ID 的依赖：新增只读 axure_live_render_page，直接调用 Axure 原生整页渲染。已检查两张完整 PNG，标题、控件和中文说明均可见。
10、Skill frontmatter 校验、Node 语法检查通过。校验器在 Windows 默认 GBK 下不能读取中文，使用 Python UTF-8 模式后通过。

## 分发与范围

包按文件白名单构建，只包含 skill、通用脚本、桥接源码/DLL、锁定依赖配置和虚构示例。未附真实 RP、私人控件库、session/token 或全局账号配置。具体文件哈希见 SHA256SUMS.txt。

上述验证来自本机独立目录，不代表同事的环境或任意新需求都已验证。自然语言到页面设计由 Agent 完成，本轮样例是在本任务中生成并执行，尚未做不同同事、不同模型的独立设计评估。当前没有实现任意 HTML/CSS 无损导入、其他 Axure build 兼容、自动创建最初的空白 RP、页面删除/重命名和复杂交互。

底层桥接沿用 0.3.1 的写入实现，本包增加只读整页渲染和工作流编排。无需新增云服务器；模型与网络依赖安装按同事自己的环境使用。
