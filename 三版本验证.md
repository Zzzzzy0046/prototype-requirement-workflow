# 1.3.0 三版本验证

验证日期：2026-09-14；Windows 本机隔离 RP；不改用户原稿。

| 项目 | 11.0.0.4134 | 11.0.0.4137 | 11.0.0.4149 |
| --- | --- | --- | --- |
| 版本选择、启动、目标文档绑定 | 通过 | 通过 | 通过 |
| 从空白 RP 新建 2 个普通功能页 | 通过 | 通过 | 通过 |
| Rectangle / Paragraph / Ellipse 原生控件与中文说明 | 通过 | 通过 | 通过 |
| 文字、位置、尺寸、字体及颜色批量更新 | 通过 | 通过 | 通过 |
| 原生撤销与重做、读取指纹恢复一致 | 通过 | 通过 | 通过 |
| 错误并发指纹被拒绝，内容未变 | 通过 | 通过 | 通过 |
| 同进程继续更新，页面/控件 ID 不变 | 通过 | 通过 | 通过 |
| 相同 scene 重复 apply 无新增写入 | 通过 | 通过 | 通过 |
| 两个功能页原生渲染输出 | 通过 | 通过 | 通过 |
| 保存关闭，新进程恢复后完整读取 | 通过 | 通过 | 通过 |
| 重开后再次 apply 写入次数为 0 | 通过 | 通过 | 通过 |

最终 Home 渲染为 752×680，Settings 为 1030×188；同一页面三版 PNG 文件 SHA256 一致，已查看 Home/Settings 原生渲染。测试 scene 为虚构示例，不是公司原稿。

“实时”在本轮指：同一个 Axure 进程、同一份已打开 RP 内连续执行原生编辑，读取模型和原生渲染反映新内容，不靠关闭重开来更新。没有使用 Computer Use 检查桌面窗口绘制；重开用于单独验证保存持久化。

机器摘要见 `compatibility-results.json`。原型 schema/说明布局自动测试为 5/5。新的旧版读取层没有完整实现官方 MCP 的交互、组件和子图信息；只声明经过验证的静态普通页工作流。

版本兼容与文件向下兼容不同：本次只验证这套基础控件样本，不保证任意更高版本创建的复杂 RP 都能在旧版打开。未知版本仍拒绝写入。

首次仍需有一份合法、已保存的空白目标 RP。通过测试不代表所有同事电脑、字体环境或任意数量控件均已验证。本包是本地实验工作流，不是 Axure 官方支持的写入 API。

## 本轮修正

- 旧版使用 .NET 8 适配器，4149 保留 .NET 10 适配器。
- 旧版使用页面的原生 package ID，不把普通对象 ID 当作页面 ID。
- 遍历分组时按对象身份去重，避免同一控件被解析多次。
- 保存使用各构建对应的原生文档字段；不只修改内存属性。
- 所有开发失败保留在本机隔离测试资料中，未将测试会话、token、RP、日志或 Axure 软件打包。

## 启动环境问题

本机曾在旧版启动时出现 `Requested Clipboard operation did not succeed`；当时普通只读 probe 也无法打开 RP。用户恢复桌面状态后重新测试成功。原型传输没有用剪贴板；遇到同样错误应检查 Windows 解锁与 Axure 自身启动状态，不重复启动或清空用户剪贴板。
